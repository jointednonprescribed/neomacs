
(add-to-list 'custom-theme-load-path "~/.emacs.d/atom-one-dark-theme/")

(when (memq window-system '(mac ns x))
  (exec-path-from-shell-initialize))

(load-file "~/neomacs/neomacs.el")

(load-theme 'atom-one-dark t)
